Confirmación breve abajo al centro después de cada acción ("R04 creado en Carpa 1", "2ª oleada de R02 guardada: 1,20 kg").

```jsx
<Toast message={toast} onDone={() => setToast(null)} />
```

Una sola a la vez; nombrá el registro y el resultado.
